# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ukaht

# Storage — set to 'local' for dev, 's3' for prod
STORAGE_TYPE=local
LOCAL_IMAGE_BASE=C:/Users/ruthr/D/team27-UKAHT/UK Antarctic Heritage Trust Data

# AWS S3 (only needed when STORAGE_TYPE=s3)
AWS_REGION=eu-west-2
AWS_BUCKET_NAME=ukaht-images
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Python executable path
PYTHON_PATH=python

# Server
PORT=3000
